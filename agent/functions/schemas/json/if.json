{
  "id": "/if",
  "$schema": "http://json-schema.org/draft-04/schema#",
  "title": "$if",
  "group": "Comparison and Flow Control",
  "description": "Evaluates a Boolean expression, and returns one of two values based on its result",
  "return": {
    "type": "any",
    "description": "`true` if the value is anomalous"
  },
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "condition": {
      "type": "boolean",
      "description": "The condition to check"
    },
    "then": {
      "description": "The value to return if the condition evaluates to `true`"
    },
    "else": {
      "description": "The value to return if the condition evaluates to `false`"
    }
  },
  "required": [
    "condition", "then", "else"
  ]
}